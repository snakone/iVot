export class Anybody {

  name: string;
  email: string;
  token: string;
  icon: string;
  address: string;

  constructor(name: string, email: string,
              token: string, icon: string, address: string){

          this.name = name;
          this.email = email;
          this.token = token;
          this.icon = icon;
          this.address = address;

          }
}
